name: Test and Build

enable-openid-connect: true

on:
  workflow_dispatch:
  push:
    branches:
      - master
    tags:
      - v*

jobs:
  test:
    runs-on: alpine
    steps:
      - uses: actions/toolkit/git-checkout@master
      - uses: actions/toolkit/go-setup@master
      - uses: actions/toolkit/go-test@master
  container:
    runs-on: alpine
    steps:
      - uses: actions/toolkit/git-checkout@master
      - uses: actions/toolkit/docker-login@master
        with:
          audience: u:1:814abaff-ffb8-4621-a283-4d7dcc136fa8
      - uses: actions/toolkit/docker-push@master
        with:
          ghcr: true
          ghcr-token: ${{ secrets.GHCR_TOKEN }}
  release:
    if: github.ref_type == 'tag'
    runs-on: alpine
    needs:
      - test
      - container
    steps:
      - uses: actions/toolkit/git-checkout@master
      - uses: actions/toolkit/docker-login@master
        with:
          audience: u:1:814abaff-ffb8-4621-a283-4d7dcc136fa8
      - uses: actions/toolkit/docker-push@master
        with:
          ghcr: true
          ghcr-token: ${{ secrets.GHCR_TOKEN }}
